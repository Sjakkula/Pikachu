package com.example.bitbyte;

import android.app.Activity;
import android.content.Intent;
import android.os.Bundle;
import android.view.View;
import android.view.View.OnClickListener;
import android.widget.Button;
import android.widget.EditText;
import android.widget.Toast;

public class ProfileScreen extends Activity {
	@Override
	protected void onCreate(Bundle savedInstanceState) {
		// TODO Auto-generated method stub
		super.onCreate(savedInstanceState);
		setContentView(R.layout.activity_profilescreen_page);

		/*
		 * Button b = (Button) findViewById(R.id.SignIn); Button n = (Button)
		 * findViewById(R.id.NewUser); final EditText t1 = (EditText)
		 * findViewById(R.id.email_address); final EditText t2 = (EditText)
		 * findViewById(R.id.Password);
		 */

	}
}
